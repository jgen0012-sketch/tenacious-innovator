# 复制此文件为 .env.local 并填写真实值
# NEVER 将 .env.local 提交到 Git

# 后端 API 地址
VITE_API_BASE_URL=http://localhost:8080

# Cloudflare Pages 会自动注入以下变量（无需手动设置）
# CF_PAGES_BRANCH    - 当前分支名
# CF_PAGES_COMMIT_SHA - 当前 commit hash
# CF_PAGES_URL       - 本次部署的预览 URL
